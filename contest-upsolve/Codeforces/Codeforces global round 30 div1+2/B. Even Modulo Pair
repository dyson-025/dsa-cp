explanation: https://www.youtube.com/live/yUEXj2WzLYs?si=taJmXyAUVukAh_aD
link: https://codeforces.com/contest/2164/problem/B

void solve() {
  ll n; cin>>n;
  vector<ll> v(n);
  for(auto &it: v) cin>>it;
  if(n<=100){
    bool found=false;
    for(int i=0;i<n;i++){
        for(int j=i+1;j<n;j++){
            if((v[j]%v[i])%2==0){
                found=true;
                cout<<v[i]<<" "<<v[j]<<endl;
                return;
            }
        }
    }
    if(!found) cout<<-1<<endl;
    return;
  }
  vector<ll> ev;
  for(auto &it: v) if(it%2==0) ev.push_back(it);
  if(ev.size()>2) cout<<ev[0]<<" "<<ev[1]<<endl;
  if(ev.size()>2) return;
  bool found=false;
  if(ev.size()==1){
    for(int i=0;i<n;i++){
        if(v[i]==ev[0]) continue;
        if((v[i]%ev[0])%2==0) {cout<<ev[0]<<" "<<v[i]<<endl;
            found=true;
            break;
        }
    }
    if(found) return;
  }
  for(int i=0;i+1<n;i++){
    if((v[i+1]%v[i])%2==0){
        cout<<v[i]<<" "<<v[i+1]<<endl;
        found=true;
        break;
    }
  }
  if(!found) cout<<-1<<endl;
}
